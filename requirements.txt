# Spool — 3D Print Workflow Manager
# Only runtime dependency is Flask. The WSGI server (waitress) is optional:
# run.py falls back to Flask's built-in server when it isn't installed.
Flask>=3.0
waitress>=3.0
