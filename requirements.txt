# My 3D Workbench — 3D Prints Management
# Only runtime dependency is Flask. The WSGI server (waitress) is optional:
# the CLI falls back to Flask's built-in server when it isn't installed.
Flask>=3.0
waitress>=3.0
